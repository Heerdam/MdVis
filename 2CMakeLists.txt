cmake_minimum_required(VERSION 3.17.2)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED True)

project(MdVis)

include_directories(MdVis PUBLIC "${CMAKE_SOURCE_DIR}/src")

add_executable(mdvis src/Gl.cpp src/main.cpp)

add_subdirectory("${CMAKE_SOURCE_DIR}/libs/glm-0.9.9.8")
target_link_libraries(glm)

add_subdirectory("${CMAKE_SOURCE_DIR}/libs/glew-2.1.0/build/cmake")
add_compile_definitions(GLEW_STATIC)
target_link_libraries(glew_s)


set(GLFW_BUILD_EXAMPLES OFF CACHE BOOL "Build the GLFW example programs" FORCE)
set(GLFW_BUILD_TESTS OFF CACHE BOOL "Build the GLFW test programs" FORCE)
set(GLFW_BUILD_DOCS OFF CACHE BOOL "Build the GLFW documentation" FORCE)
set(GLFW_INSTALL OFF CACHE BOOL "Generate installation target" FORCE)

add_subdirectory("${CMAKE_SOURCE_DIR}/libs/glfw-3.3.2")
target_link_libraries(glfw)
